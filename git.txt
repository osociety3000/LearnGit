# git --version

# git config --global user.name "osociety3000"

# git config --global user.email "osociety3000@gmail.com"

# git config --list

# git init

# git add .

# git status

# git log   <> git log --oneline

# git show <commit_ID>

# git remote add origin <centralgit url>

# git push -u origin master (Enter Username & Password)

# git pull origin master

# git branch -M main

# git branch

# git branch <branch_name>  // create an new branches

# git checkout <branch_name>  // Switch branches

# git branch -d <branch_name>  // Delete branches

# git merge <branch_name>    // merge branches

# git stash

# git stash list

# git stash apply stash@{0}

# git stash clear

# git reset <file_name>     // To rest staging area

# git reset .               // before commit

# git reset --hard      // To reset the changes from both staging and working directory at a time

# git revert <commit_ID>

# git clean -n          // dry run

# git clean -f             // forcefully

# git tag -a <tag_name> -m <message> <commit_ID>    // apply tag

# git tag

# git show <tag_name>

# git tag -d <tag_name>     // Delete a tag

# git clone <URL_of_GitHub_Repo.>
